/**
 * SEO Metadata for NASH MASH Mouse Models
 * Auto-generated for canonical URLs and structured data
 */

import { generateMetadata, generateBreadcrumbs } from '@/lib/seo';

export const metadata = generateMetadata({
  title: 'NASH MASH Mouse Models',
  description: 'NASH MASH Mouse Models from ingenious targeting laboratory. Custom mouse model engineering since 1998.',
  path: '/nash-mash-mouse-models',
});

// BreadcrumbList structured data
export const breadcrumbSchema = generateBreadcrumbs({
  items: [
    { name: 'Home', path: '/' },
    { name: 'NASH MASH Mouse Models', path: '/nash-mash-mouse-models' },
  ],
});
