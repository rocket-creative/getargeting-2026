/**
 * SEO Metadata for Obesity Mouse Models
 * Auto-generated for canonical URLs and structured data
 */

import { generateMetadata, generateBreadcrumbs } from '@/lib/seo';

export const metadata = generateMetadata({
  title: 'Obesity Mouse Models',
  description: 'Obesity Mouse Models from ingenious targeting laboratory. Custom mouse model engineering since 1998.',
  path: '/obesity-mouse-models',
});

// BreadcrumbList structured data
export const breadcrumbSchema = generateBreadcrumbs({
  items: [
    { name: 'Home', path: '/' },
    { name: 'Obesity Mouse Models', path: '/obesity-mouse-models' },
  ],
});
